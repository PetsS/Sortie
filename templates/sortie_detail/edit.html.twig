{% extends 'base.html.twig' %}

{% block title %}Création sortie - Sortie.com{% endblock %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}

    <h1 class="display-6 text-center">Créer une sortie</h1>

    <div class="d-flex justify-content-center">
        <div class="border border-0 rounded p-3"
             style="
                margin-top: 1rem;
                margin-bottom: 1rem;
                background-color: rgba(255,255,255,0.16);
                "
        >
            {{ form_start(form) }}

            {{ form_widget(form) }}
            {{ form_errors(form) }}

            {#    <div class="btn-group" role="group" aria-label="Basic mixed styles example"> #}
            {#        <button type="submit" class="btn btn-primary">Page suivante</button> #}
            {#        <a href="{{ path('app_sortie_liste') }}" class="btn btn-danger">Annuler</a> #}
            {#    </div> #}

            <a class="btn btn-secondary" type="button" href="{{ path('app_sortie_liste') }}">Retour</a>
            <button class="btn btn-primary" type="submit">Page suivante</button>

        </div>
    </div>

    {{ form_end(form) }}
{% endblock %}