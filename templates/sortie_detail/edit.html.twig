{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}

    {{ form_start(form) }}
    <button type="submit">Envoyer</button>
    {{ form_label(form) }}
    {{ form_widget(form) }}
    {{ form_errors(form) }}


    {{ form_end(form) }}
{% endblock %}