{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}

    <h1>Mettre à jour {{ user.pseudo }}</h1>

    {{ form_start(form) }}

    {{ form_row(form.pseudo) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.prenom) }}
    {{ form_row(form.telephone) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password) }}
    {{ form_row(form.photo) }}
    {{ form_row(form.site) }}
    {{ form_row(form.isActif) }}


    {{ form_widget(form.return) }} {{ form_widget(form.submit) }}
{#    {% if is_granted('ROLE_ADMIN') %}#}
{#        {{ form_row(form.delete) }}#}
        <a href="{{ path('app_profil_supprimer', {'id': user.id}) }}" class="btn btn-danger" onclick="return confirmDelete();">Supprimer</a>
        <script>
            function confirmDelete() {
                return confirm("Etes-vous sûr de vouloir supprimer ce compte?");
            }
        </script>
{#    {% endif %}#}

    {{ form_end(form) }}



{% endblock %}